"""Dense vector index backed by LanceDB, behind a store interface (Phase 3).

The interface must support metadata filtering from day one — its absence is a
verified gap in the ARP implementation this repo exists to close (ADR-0001).
"""
