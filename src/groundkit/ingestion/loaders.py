"""Loaders for md/txt/pdf/html from files, directories, and URLs (Phase 1).

Directory-scale ingestion is a v1 requirement; path containment mirrors the
``allowed_base_dir`` discipline carried over from ARP's loaders (ADR-0001).
"""
