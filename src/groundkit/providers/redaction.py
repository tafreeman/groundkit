"""Anonymization at the LLM boundary: redaction pass applied before any text is
sent to a cloud provider (Phase 5) — names are replaced with tokens, and the
patterns are configurable. Local mode sends nothing anywhere.
"""
